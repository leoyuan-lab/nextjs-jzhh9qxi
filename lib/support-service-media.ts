/** Homepage Support twin — orbital sunrise loop. */
export const SUPPORT_TWIN_VIDEO_SRC = '/applications/video/sunrise-from-space.mp4';

/** Support /service page — same orbital sunrise clip as homepage twin. */
export const SUPPORT_SERVICE_ATMOSPHERE_VIDEO = SUPPORT_TWIN_VIDEO_SRC;

/** Support /service — poster + static fallback (frame from same clip). */
export const SUPPORT_SERVICE_ATMOSPHERE_FRAME = '/images/support/support-sunrise-from-space-frame.jpg';
